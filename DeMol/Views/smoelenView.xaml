<UserControl x:Class="DeMol.Views.SmoelenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeMol.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>

            <RowDefinition Height="1*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        
        <UniformGrid Grid.Row="1" Grid.Column="1"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <Button Name="vic" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/vic.png"/>
            </Button>
            <Button Name="juul" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/juul.png"/>
            </Button>
            <Button Name="staf" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/staf.png"/>
            </Button>
            <Button Name="mie" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/mie.png"/>
            </Button>
            <Button Name="jochen" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/jochen.png"/>
            </Button>
            <Button Name="hilde" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/hilde.png"/>
            </Button>
            <Button Name="kris" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/kris.png"/>
            </Button>
            <Button Name="pieter" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/pieter.png"/>
            </Button>
            <Button Name="hanne" Style="{StaticResource imgButton}">
                <Image Source="./smoelen/hanne.png"/>
            </Button>

        </UniformGrid>
    </Grid>
</UserControl>

    