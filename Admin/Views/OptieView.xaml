<UserControl x:Class="Admin.Views.OptieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Admin.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <!--<RadioButton Style="{StaticResource R1}" Content="sdfsdfsdf" IsChecked="true"/>
        <RadioButton Style="{StaticResource R1}" Content="sdfsdfsdf" IsChecked="false"/>-->
        <RadioButton GroupName="qasdasd" Style="{StaticResource R1}" Content="{Binding Path=OptieText}" IsChecked="{Binding Path=IsSelected}"/>

    </StackPanel>
</UserControl>
